import { UPDATE_ACCOUNT_FIELD, UPDATE_SETTINGS_FIELD } from '../actions/log';

// eslint-disable-next-line no-unused-vars
/*const filterActions = (store) => (next) => (action) => {
  if (action.type === UPDATE_ACCOUNT_FIELD || action.type === UPDATE_SETTINGS_FIELD) {
    if (action.identifier === 'password' || action.identifier === 'secondPassword' || action.identifier === 'Newpassword') {
      // regex qui remplace chaque valeur de l'action par * : /./g  le point est le joker pour dire
      // n'importe quels caractéres et le g est une global pour dire pas juste le 1er caractére de
      // la chaine mais tous.
      // action.value = action.value.replace(/./g, '*');
    }
  }
  next(action);
};*/

// export default filterActions;
